<Module 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="../FrankConfig.xsd">

	<Adapter name="BrpPersonenUpdateScheduler"
		description="Brp Personen Update Scheduler processor"
		active="${brp.update.enabled}">

		
        <Receiver name="BrpPersonenUpdateScheduler">
            <JavaListener name="BrpPersonenUpdateScheduler" />
        </Receiver>

		<Pipeline>
			<Exits>
				<Exit name="EXIT" state="SUCCESS" />
				<Exit name="EXCEPTION" state="ERROR" />
			</Exits>
			
            <SenderPipe name="GetWijzigingenFromDate" getInputFromFixedValue="&lt;dummy/&gt;">
                <IbisLocalSender name="BrpGetWijzigingen" javaListener="BrpGetWijzigingen" />
				<Param name="date" namespaceDefs="xs=http://www.w3.org/2001/XMLSchema" xpathExpression="format-date(current-date() - xs:dayTimeDuration('P1D'), '[Y]-[M01]-[D01]')"/>
                <Forward name="success" path="JsonToXml" />
                <Forward name="exception" path="EXCEPTION" />
                <Forward name="timeout" path="EXCEPTION" />
            </SenderPipe>

			<JsonPipe name="JsonToXml" storeResultInSessionKey="xmlbody" />

			<!-- Update subscriptions to historic bsns to current bsn -->
			<ForEachChildElementPipe name="SubscribtionIterator" getInputFromSessionKey="xmlbody" 
				storeResultInSessionKey="haveSubscriptions"
				elementXPathExpression="root/burgerservicenummers">
                <FixedQuerySender
					name="getPersonenSender"
                    queryType="SELECT"
					query="SELECT ?{bsn} FROM subscripties WHERE bsn=?{bsn} AND eind_datum IS NULL"
					includeFieldDefinition="false">
					<Param name="bsn" xpathExpression="./burgerservicenummers"/>
            	</FixedQuerySender>
				<Forward name="success" path="FilterOnlySubscribedBSN"/>
				<Forward name="exception" path="EXCEPTION"/>
			</ForEachChildElementPipe>

			
			<XsltPipe name="FilterOnlySubscribedBSN"
				styleSheetName="xsl/FilterSubscribersBsn.xslt"
				storeResultInSessionKey="FilteredBSN">
				<Forward name="success" path="bsnUpdaterIterator" />
			</XsltPipe>
			
			<!-- Update subscriptions to historic bsns to current bsn -->
			<ForEachChildElementPipe name="bsnUpdaterIterator" getInputFromSessionKey="FilteredBSN"
				elementXPathExpression="burgerservicesnummers/bsn" parallel="true">
				<MessageStoreSender slotId="${instance.name}/updateBsnStore" />
				<Forward name="success" path="EXIT"/>
				<Forward name="exception" path="EXCEPTION"/>
			</ForEachChildElementPipe>

		</Pipeline>
	</Adapter>

    <Adapter
        name="AutoPersonenUpdateScheduler"
		active="${brp.update.enabled}"
        description=""
        >
        <Receiver name="AutoRetry_WorkflowSelector">
            <JavaListener name="AutoRetry_PersonenUpdateScheduler"/>
        </Receiver>

        <Pipeline>
            <Exits>
                <Exit name="EXIT" state="SUCCESS" />
                <Exit name="EXCEPTION" state="ERROR" />
            </Exits>

            <SenderPipe
                name="SetDuplicatesToMaxRetry"
                >
				<FixedQuerySender
					name="SetDuplicatesToMaxRetry" 
					query="WITH ranked AS (
							SELECT 
								MESSAGEID,
								ROW_NUMBER() OVER (
									PARTITION BY MESSAGE
									ORDER BY MESSAGEDATE DESC
								) AS rn
							FROM IBISSTORE
							WHERE TYPE = 'E'
							AND SLOTID = ?{slotId}
							AND (
									LABEL IS NULL 
									OR CAST(LABEL AS NUMERIC) &lt; ?{maxRetries}
								)
						)

						UPDATE IBISSTORE s
						SET LABEL = (?{maxRetries})::text
						FROM ranked r
						WHERE s.MESSAGEID = r.MESSAGEID
						AND r.rn > 1;"
                    />
                    <Param name="slotId" value="${instance.name}/updateBsnStore" />
                    <Param name="maxRetries" value="${MAX_RETRIES}" type="INTEGER" />
			</SenderPipe>

            <SenderPipe
                name="UpdateErrorMessagesToMessagesSender"
                >
				<FixedQuerySender
					name="UpdateErrorMessagesToMessagesFixedQuerySender" 
					query="UPDATE IBISSTORE s
							SET 
								TYPE = 'M',
								LABEL = (
									CASE 
										WHEN s.LABEL IS NULL THEN '1'
										ELSE (CAST(s.LABEL AS NUMERIC) + 1)::text
									END
								)
							WHERE s.TYPE = 'E'
							AND s.SLOTID = ?{slotId}
							AND (
									s.LABEL IS NULL 
									OR CAST(s.LABEL AS NUMERIC) &lt; ?{maxRetries}
								);"
                    />
                    <Param name="slotId" value="${instance.name}/updateBsnStore" />
                    <Param name="maxRetries" value="${MAX_RETRIES}" type="INTEGER" />
			</SenderPipe>
        </Pipeline>
    </Adapter>

	<Scheduler>
        <SendMessageJob name="BrpPersonenUpdateSchedulerJob"
            active="${brp.update.enabled}"
            javaListener="BrpPersonenUpdateScheduler"
            cronExpression="0 0 2 ? * * *">
			<Locker objectId="BrpPersonenUpdateScheduler-Lock" retention="1" />
        </SendMessageJob>
		
        <SendMessageJob name="AutoRetry_PersonenUpdateSchedulerJob"
            active="${brp.update.enabled}"
            javaListener="AutoRetry_PersonenUpdateScheduler"
            cronExpression="0 0 5 ? * * *">
			<Locker objectId="AutoRetry_PersonenUpdateScheduler-Lock" retention="1" />
        </SendMessageJob>
	</Scheduler>
</Module>