CREATE TABLE IF NOT EXISTS public.personen
(
    burgerservicenummer integer,
    body text COLLATE pg_catalog."default",
    appid text COLLATE pg_catalog."default",
    date timestamp without time zone
)